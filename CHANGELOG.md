# Changelog

灯控中心的版本更新记录。版本号格式：`v<主>.<次>`，每次公开发布递增。

## v1.0.5 — 2026-09-15

### 修复

- **面板设置全部失效（致命，静默失败）**：面板里的 `ledctl` 路径写死成了宿主机路径 `/usr/local/sbin/ledctl`，
  容器内根本不存在 → 12 个写接口（电源灯 / 硬盘灯 / 夜间 / 时间表 / 预设 / 参数 / 演示 / 自检 / 熄灯 / 重启…）
  全部执行失败，界面却仍弹「已启用」的成功文案，用户完全无感。现按容器内路径解析，并支持 `LEDCTL`
  环境变量覆盖；失败时直接回显真实错误，不再假装成功。
- **非法时间段被静默接受**：面板原先只校验字符集，`25:00-30:00` 这类值能写进配置，
  引擎解析时静默跳过 → 永远不生效，用户只看到"设了没用"。现在面板与 CLI 两端都校验
  小时 00-23、分钟 00-59、起止不能相同。
- **时间段清不掉**：面板传空值时后端走"不带窗口参数=保留旧值"，用户永远清不干净。现支持显式清空。
- **并发保存互相覆盖**：面板保存时会同时发电源灯与硬盘灯两组时间表，而 `ledctl` 是"读-改-写"，
  后写者用旧快照覆盖前者（实测 5/5 轮丢更新）。现改为串行保存，配置读写加 `fcntl.flock` 文件锁。
- **引擎越界时间静默丢弃**：`parse_windows` 增加范围过滤，越界项直接丢弃，
  不留"看着配了却不生效"的假象。

### 变更

- 硬盘灯总开关为「熄灭」时，面板页头提示"需切到跟随系统后定时才会改变显示"。

## v1.0.4 — 2026-09-15

### 变更

- 作者署名统一为「西了个瓜」（包含 v1.0.3 的时区修复）。

## v1.0.3 — 2026-09-15

### 修复

- **夜间定时整体错位**：容器时区为 UTC，夜间计划按 UTC 判定，实际偏移 8 小时。
  现注入 `TZ=Asia/Shanghai` 并只读挂载宿主机 `localtime` / `zoneinfo` 修正
  （不改动宿主机时区设置）。

## v1.0.2 — 2026-09-14

### 修复

- **面板网页图标空白**：补上 favicon。
- **应用中心图标空白**：注册时的 `icon_url` 误用了回环地址，改为 NAS 真实 IP。

## v1.0.1 — 2026-09-14

### 修复

- 应用中心安装包体积字段写入真实值（此前为占位值，界面显示不准）。
- 打包补齐 `.sh` 可执行位，避免 NAS 端 `permission denied`。

## v1.0.0 — 2026-09-14

### 首个公开发行版

- **容器化灯控**：灯控引擎与 Web 面板都跑在容器内，特权 + host 网络直写 LED 寄存器。
- **电源灯**：常亮 / 呼吸（周期与最低亮度可调）/ 熄灭。
- **硬盘灯**：跟随系统读写灯语 / 常亮 / 心跳 / 呼吸 / 熄灭。
- **夜间计划**：电源灯与硬盘灯各自独立时间表，按时间段自动切换方案。
- **常用方案一键切换**：预设方案一键套用。
- **配置持久化**：所有设置写在 `data/`，容器升级重建不丢。
